{
  "private": true,
  "workspaces": [
    "packages/*",
    "site"
  ],
  "scripts": {
    "develop": "yarn workspace site develop",
    "clean": "yarn workspace site clean",
    "test": "yarn workspace gatsby-transformer-cloudinary test",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "prettier": "2.6.2"
  }
}
